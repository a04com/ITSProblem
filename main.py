from classes import Player, Item, Events
import random

player = Player("10 единиц")
item = Item(10)

while True:
    while player.MAX_LOAD > 0:
        
    # randomEventNumber = random.randint(0, 8)

    # event = Events(randomEventNumber).name
